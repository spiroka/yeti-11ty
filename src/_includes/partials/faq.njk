<section class="container">
  <h2>Gyakori kérdések</h2>
  <dl class="faq">
    {% for item in faq %}
      <dt>{{ item.question }}</dt>
      <dd>{{ item.answer | safe }}</dd>
    {% endfor %}
  </dl>
</section>